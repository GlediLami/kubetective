# build output
/bin/
*.exe

# incidents recorded by kubedoctor
~/.kubedoctor/

# editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# private working docs - deliberately unpublished. docs/ itself is public
# (README links into it), so the private files are named individually rather
# than ignoring the whole directory.
/docs/HANDOFF.md
/docs/ROADMAP.md
/docs/promotion-notes.md
/docs/engineering-review.html
/prompt.txt
